syntax = "proto3";

package common;

message Empty {}

message Pagination {
    int32 limit = 1;
    int32 page = 2;
    string sort = 3;
    string order = 4;
}

message PaginationResponse {
    int32 total = 1;
    int32 limit = 2;
    int32 page = 3;
    string sort = 4;
    string order = 5;
}